{% extends 'base.html.twig' %}

{% block title %}Edit Utilisateur{% endblock %}

{% block body %}
<div class="container  ">
  
    

<div class="container  ">
<h4 class="  mb-4 text-lg font-semibold text-gray-600 dark:text-gray-300"></h4>
 <h4 class="  mb-4 text-lg font-semibold text-gray-600 dark:text-gray-300">
               Modifier l'utilisateur
    </h4>
 

    {{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
                                <div class="col-5">
                                        <label for="yourName" class="form-label">Nom</label>

                                        <div class="invalid-feedback">Veuillez entrer votre nom </div>
                                    {{ form_widget(form.nom,{'attr':{'class':'form-control'}}) }}
                                     {{ form_errors(form.nom) }}

                                </div>
                                <div class="col-5">
                                        <label for="yourName" class="form-label"> Prenom</label>

                                        <div class="invalid-feedback">Veuillez entrer votre prenom!</div>
                                    {{ form_widget(form.prenom,{'attr':{'class':'form-control'}}) }}
                                     {{ form_errors(form.prenom) }}

                                </div>


                                    <div class="col-5">
                                        <label for="yourEmail" class="form-label">Adresse-Email</label>
                                        {{ form_widget(form.adresse,{'attr':{'class':'form-control'}}) }}
                                        {{ form_errors(form.adresse) }}
                                        <div class="invalid-feedback">Veuillez entrer votre adresse email!</div>
                                    </div>



                                    <div class="col-5">
                                        <label for="yourPassword" class="form-label">Mot de passe</label>
                                        {{ form_widget(form.mdp,{'attr':{'class':'form-control'}}) }}
                                        <div class="invalid-feedback">Veuillez entrer votre mot de passe !</div>
                                         {{ form_errors(form.mdp) }}
                                    </div>

                                    
                                    <div class=" center col-2 mt-3">
                                      <button class="block w-full px-4 py-2 mt-4 text-sm font-medium leading-5 text-center text-white transition-colors duration-150 bg-purple-600 border border-transparent rounded-lg active:bg-purple-600 hover:bg-purple-700 focus:outline-none focus:shadow-outline-purple" 
                                      href="{{path('app_utilisateur_index')}}" class="btn btn-primary center">{{ button_label|default('Editer')  }}</button>
                                    </div>
                                    

                                     
                                {{ form_end(form) }}

    

    <a class="btn btn-link" href="{{ path('app_utilisateur_index') }}">Retour à la liste</a>
    </div>
    

   


{% endblock %}